# Supabase Configuration (optional)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE=your_supabase_service_role_key

# CSRF Protection (required)
CSRF_SECRET=your_32_character_random_string

# Encryption Key (required for BYOK if enabled)
ENCRYPTION_KEY=your_base64_encryption_key

# AI Model API Key (required)
OPENROUTER_API_KEY=your_openrouter_api_key

# Groq API Key (optional - for voice input)
# Get your FREE API key at https://console.groq.com/keys
# Enables: Speech-to-Text (Whisper) for voice input
GROQ_API_KEY=your_groq_api_key

# LinkUp Search API (required for prospect research)
# Get your API key at https://linkup.so
# Powers all web research (chat, batch, deep research)
# Standard mode: $0.005/search, Deep mode: $0.02/search
# LinkUp is enabled by default when API key is set - no additional flag needed
LINKUP_API_KEY=your_linkup_api_key

# Google AI API Key (optional - for beta Gemini Grounded Search)
# Get your API key at https://makersuite.google.com/app/apikey
# Enables beta feature: Gemini Grounded Search with native Google Search citations
# Only available to Scale plan users with beta features enabled
# Cost: ~$0.035 per 1,000 prompts (Gemini 2.0 Flash pricing)
GOOGLE_AI_API_KEY=your_google_ai_api_key

# Roboflow Object Detection (optional - for enhanced CAPTCHA solving)
# Get your FREE API key at https://app.roboflow.com/settings/api
# Enables hybrid CAPTCHA solving: Roboflow (precise coordinates) + VLM (semantic reasoning)
# Improves CAPTCHA solve rate from ~70% to ~95%
ROBOFLOW_API_KEY=your_roboflow_api_key
# Optional: Use a custom trained model (default: hcaptcha-challenger)
# ROBOFLOW_MODEL_ID=hcaptcha-challenger
# ROBOFLOW_MODEL_VERSION=2

# FEC API (optional - Federal Election Commission political contributions)
# Get your FREE API key at https://api.data.gov/signup/
# Rate limit: 1,000 requests/hour with API key
# Best for: searching individual political contributions, donor research, wealth indicators
FEC_API_KEY=your_fec_api_key

# US Government Data APIs (no API key required for most endpoints)
# USAspending, Treasury Fiscal Data, and Federal Register APIs are FREE
# Get an optional api.data.gov key for higher rate limits: https://api.data.gov/signup/
# Best for: federal contracts/grants (USAspending), national debt (Treasury), regulations (Federal Register)
DATA_GOV_API_KEY=your_data_gov_api_key

# SEC EDGAR API (no API key required)
# The sec-edgar-api package fetches directly from SEC EDGAR
# Automatically throttled to 10 requests/second
# Best for: 10-K/10-Q filings, financial statements, executive compensation

# Neon CRM (optional - for admin/system-level direct API access)
# NOTE: Most users should add credentials via the Settings → Integrations UI instead!
# These env vars enable system-level direct API tools (neon_crm_search_accounts, etc.)
# Get your credentials from Neon CRM:
# - Org ID: Settings > Organization Profile > Account Information
# - API Key: Settings > User Management > [User] > API Access
# API Docs: https://developer.neoncrm.com/api-v2/
NEON_CRM_ORG_ID=your_neon_crm_org_id
NEON_CRM_API_KEY=your_neon_crm_api_key
# Set to "true" if using a Neon CRM trial instance (uses trial.neoncrm.com)
NEON_CRM_TRIAL=false

# Google Workspace Integration (optional - Gmail & Google Drive)
# Create OAuth credentials at https://console.cloud.google.com
# 1. Create project, enable Gmail API + Drive API + Picker API
# 2. Create OAuth 2.0 credentials (Web application)
# 3. Add redirect URI: https://your-domain.com/api/google-integrations/callback
# 4. Create API Key (APIs & Services > Credentials > Create Credentials > API Key)
#    - Restrict to: Google Picker API only
#    - Add HTTP referrers: your domain (e.g., https://your-domain.com/*)
# 5. Find your App ID: IAM & Admin > Settings > Project number (numeric)
# Features: Read inbox via AI chat, create email drafts (no auto-send), import Drive documents
# Scopes: gmail.readonly, gmail.compose (NO gmail.send), drive.file
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Picker API Key (required for Google Drive file picker)
# This is a Browser API Key restricted to the Picker API
GOOGLE_PICKER_API_KEY=your_picker_api_key
# App ID = Cloud Project Number (found in IAM & Admin > Settings)
# This is a numeric value like 123456789012
GOOGLE_APP_ID=your_cloud_project_number
# Feature flags (optional - all enabled by default when configured)
GOOGLE_INTEGRATION_ENABLED=true
# GMAIL_ENABLED=true
# DRIVE_ENABLED=true
# STYLE_ANALYSIS_ENABLED=true
# GOOGLE_ROLLOUT_PCT=100

# Notion Integration (optional - import Notion pages for RAG search)
# Create a Notion integration at https://www.notion.so/my-integrations
# 1. Click "New integration"
# 2. Set name, select workspace
# 3. Under "Capabilities", enable:
#    - Read content (required)
#    - Read user information (optional)
#    - No user information (recommended for privacy)
# 4. Copy the "Internal Integration Token" (starts with secret_)
# 5. Get OAuth Client ID and Secret from the integration's OAuth tab
# Features: Import Notion pages and databases for AI-powered semantic search
# Note: Users must share pages with the integration in Notion
NOTION_CLIENT_ID=your_notion_oauth_client_id
NOTION_CLIENT_SECRET=your_notion_oauth_client_secret

# Autumn Subscriptions (optional - for subscription management)
# Get your TEST API key from https://app.useautumn.com/sandbox/dev
# For production, use your live key from https://app.useautumn.com/live/dev
AUTUMN_SECRET_KEY=your_autumn_secret_key

# PostHog Analytics (optional - for product analytics and user behavior tracking)
# Get your API key from https://posthog.com
# person_profiles: 'identified_only' - Only creates profiles for logged-in users
# capture_pageview: false - Manual pageview tracking for better control
# autocapture: Captures clicks and form submits on buttons and links
NEXT_PUBLIC_POSTHOG_KEY=your_posthog_api_key
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
NEXT_PUBLIC_POSTHOG_ENABLE_RECORDINGS=false

# Production Configuration (optional)
NEXT_PUBLIC_VERCEL_URL=your_production_domain

# Development Tools (optional)
ANALYZE=false
